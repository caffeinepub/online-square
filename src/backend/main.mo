actor {
  public query ({ caller }) func getInfo() : async Text {
    "Welcome to Online Square Digital Service Center. This is a static backend for the landing page. No dynamic data is stored or processed.";
  };
};
